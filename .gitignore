# --- Dependencies (Зависимости) ---
node_modules/
# Игнорирует node_modules везде, включая client/node_modules и server/node_modules

# Yarn Plug'n'Play (из файла client)
/.pnp
.pnp.js

# Lock files (из твоего списка, хотя обычно их советуют коммитить)
pnpm-lock.yaml
package-lock.json

# --- Environment Variables (Переменные окружения) ---
# Игнорирует .env файлы в корне и в любых подпапках (client/server)
.env
.env.local
.env.*.local
.envrc

# --- Logs (Логи) ---
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Build Outputs (Сборка) ---
# Папки сборки (работает для корня и вложенных папок, если убрать слэш в начале)
build/
dist/
coverage/

# --- IDE & OS (Системные файлы и редакторы кода) ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- Specific Client Ignores (Специфичное для клиента) ---
# Если вдруг появятся специфичные файлы только для клиента, их можно добавить так:
# client/kakoy-to-fail